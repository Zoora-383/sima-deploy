apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sima-storage
  namespace: sima
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
